# Copy this file to .env and fill in real values.
# .env is gitignored — never commit credentials.

# PostgreSQL (matches docker-compose.yml)
HENSU_DB_USER=hensu
HENSU_DB_PASSWORD=
HENSU_DB_NAME=hensu

# JWT public key path — required, no default. Use an absolute file: URL — relative paths
# break across run modes (quarkusDev working dir = hensu-server/, native runner = repo root).
# Generate your keypair first: see docs/developer-guide-server.md#local-development
# Set via shell (not .env) to resolve correctly:
#   export HENSU_JWT_PUBLIC_KEY="file:$(pwd)/dev/keys/publicKey.pem"
HENSU_JWT_PUBLIC_KEY=file:/absolute/path/to/repo/dev/keys/publicKey.pem
